function forEach(arr, func) { 
  for(let i = 0; i < arr.length; i++ {
    func(arr[i], i, arr)
  }
}
// const arr = ["a", 'b','v']
// forEach(arr, console.log)
//
//
// x[1, 2, 3, 4]
// x[6, 7, 8, 9]
